name: "iDevOps"
description: "All-in-one DevOps pipeline - Security, Linting, Dependencies, Secrets, IaC, Container, DAST, SBOM. 34+ languages. 67 scripts. Free & Open-Source."
author: "TAI-opensource"
branding:
  icon: "shield"
  color: "blue"

inputs:
  token:
    description: "GitHub token"
    default: "${{ github.token }}"
  languages:
    description: "Comma-separated: javascript,typescript,python,rust,go,java,kotlin,csharp,c,cpp,ruby,php,swift,scala,haskell,elixir,dart,shell,lua,perl,r,julia,powershell,sql,graphql,protobuf,css,html,yaml,json,markdown,terraform,cloudformation,kubernetes,helm,docker,ansible"
    default: "auto"
  security:
    description: "Run security scans"
    default: "true"
  sast:
    description: "Run SAST scans"
    default: "true"
  sca:
    description: "Run SCA scans"
    default: "true"
  secrets:
    description: "Scan for secrets"
    default: "true"
  lint:
    description: "Run linters"
    default: "true"
  lint-fix:
    description: "Auto-fix linting issues"
    default: "false"
  quality:
    description: "Run code quality checks"
    default: "true"
  dependencies:
    description: "Check dependencies"
    default: "true"
  check-outdated:
    description: "Check for outdated deps"
    default: "true"
  check-licenses:
    description: "Check license compliance"
    default: "false"
  docker:
    description: "Lint Dockerfiles"
    default: "auto"
  container-image:
    description: "Container image to scan"
    default: ""
  iac:
    description: "Scan IaC files"
    default: "auto"
  iac-types:
    description: "IaC types: terraform,cloudformation,kubernetes,helm,docker,ansible"
    default: "auto"
  dast:
    description: "Run DAST scanning"
    default: "false"
  target-url:
    description: "Target URL for DAST"
    default: ""
  recon:
    description: "Run security recon"
    default: "false"
  sbom:
    description: "Generate SBOM"
    default: "false"
  sbom-format:
    description: "SBOM format: cyclonedx, spdx, both"
    default: "both"
  fail-on:
    description: "Fail threshold: critical, high, medium, low, never"
    default: "high"
  upload-sarif:
    description: "Upload SARIF to Security tab"
    default: "true"
  report-dir:
    description: "Reports directory"
    default: ".idevops/reports"
  semgrep-app-token:
    description: "Semgrep App token for custom rules (pass via env: SEMGREP_APP_TOKEN)"
    default: ""

outputs:
  summary:
    description: "Markdown summary"
    value: "${{ steps.summary.outputs.report }}"
  score:
    description: "Health score (0-100)"
    value: "${{ steps.summary.outputs.score }}"
  sarif-files:
    description: "SARIF files"
    value: "${{ steps.sarif.outputs.files }}"

runs:
  using: "composite"
  steps:
    - name: "iDevOps - Setup"
      shell: bash
      run: |
        echo "[iDevOps] All-in-One DevOps Pipeline"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "Languages:    ${{ inputs.languages }}"
        echo "Security:     ${{ inputs.security }}"
        echo "SAST:         ${{ inputs.sast }}"
        echo "SCA:          ${{ inputs.sca }}"
        echo "Secrets:      ${{ inputs.secrets }}"
        echo "Lint:         ${{ inputs.lint }}"
        echo "Quality:      ${{ inputs.quality }}"
        echo "Dependencies: ${{ inputs.dependencies }}"
        echo "Docker:       ${{ inputs.docker }}"
        echo "IaC:          ${{ inputs.iac }}"
        echo "DAST:         ${{ inputs.dast }}"
        echo "Recon:        ${{ inputs.recon }}"
        echo "SBOM:         ${{ inputs.sbom }}"
        echo "Fail-on:      ${{ inputs.fail-on }}"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

        chmod +x ${{ github.action_path }}/scripts/*.sh 2>/dev/null || true
        mkdir -p "${{ inputs.report-dir }}"

    # ─────────────────────────────────────────────
    # SECURITY
    # ─────────────────────────────────────────────
    - name: "Security: Universal scan"
      if: inputs.security == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/security-universal.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        LANGUAGES: ${{ inputs.languages }}
        GITHUB_TOKEN: ${{ inputs.token }}
      continue-on-error: true

    - name: "Security: Trivy"
      if: inputs.security == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/security-trivy.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Security: OSV-Scanner"
      if: inputs.sca == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/security-osv.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Security: Semgrep SAST"
      if: inputs.sast == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/security-semgrep.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        LANGUAGES: ${{ inputs.languages }}
        SEMGREP_APP_TOKEN: ${{ inputs.semgrep-app-token }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # SECRETS
    # ─────────────────────────────────────────────
    - name: "Secrets: Gitleaks"
      if: inputs.secrets == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/secrets-gitleaks.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        GITHUB_TOKEN: ${{ inputs.token }}
      continue-on-error: true

    - name: "Secrets: TruffleHog"
      if: inputs.secrets == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/secrets-trufflehog.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # LINTING
    # ─────────────────────────────────────────────
    - name: "Lint: JavaScript/TypeScript"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'javascript') || contains(inputs.languages, 'typescript') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-javascript.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        FIX: ${{ inputs.lint-fix }}
      continue-on-error: true

    - name: "Lint: Python"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'python') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-python.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        FIX: ${{ inputs.lint-fix }}
      continue-on-error: true

    - name: "Lint: Rust"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'rust') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-rust.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Go"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'go') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-go.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Java/Kotlin"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'java') || contains(inputs.languages, 'kotlin') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-java.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: C/C++"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'c') || contains(inputs.languages, 'cpp') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-c-cpp.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Ruby"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'ruby') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-ruby.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: PHP"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'php') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-php.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Swift"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'swift') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-swift.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Scala"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'scala') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-scala.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Haskell"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'haskell') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-haskell.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Elixir"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'elixir') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-elixir.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Dart"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'dart') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-dart.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Shell"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'shell') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-shell.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Lua"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'lua') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-lua.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Perl"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'perl') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-perl.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: R"
      if: inputs.lint == 'true' && inputs.languages != 'auto'
      shell: bash
      run: |
        LANGS="${{ inputs.languages }}"
        if echo ",$LANGS," | grep -q ",r,"; then
          ${{ github.action_path }}/scripts/lint-r.sh
        fi
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: R (auto)"
      if: inputs.lint == 'true' && inputs.languages == 'auto'
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-r.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Julia"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'julia') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-julia.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: PowerShell"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'powershell') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-powershell.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: SQL"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'sql') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-sql.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: GraphQL"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'graphql') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-graphql.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Protobuf"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'protobuf') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-protobuf.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: CSS"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'css') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-css.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: HTML"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'html') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-html.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: YAML"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'yaml') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-yaml.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: JSON"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'json') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-json.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Markdown"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'markdown') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-markdown.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Docker"
      if: inputs.docker == 'true' || inputs.docker == 'auto' || (inputs.lint == 'true' && contains(inputs.languages, 'docker'))
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-docker.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Terraform"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'terraform') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-terraform.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Kubernetes"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'kubernetes') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-kubernetes.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Helm"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'helm') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-helm.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: Ansible"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'ansible') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-ansible.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    - name: "Lint: C#"
      if: inputs.lint == 'true' && (contains(inputs.languages, 'csharp') || inputs.languages == 'auto')
      shell: bash
      run: ${{ github.action_path }}/scripts/lint-csharp.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # IaC
    # ─────────────────────────────────────────────
    - name: "IaC: Scan"
      if: inputs.iac == 'true' || inputs.iac == 'auto'
      shell: bash
      run: ${{ github.action_path }}/scripts/iac-scanner.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        IAC_TYPES: ${{ inputs.iac-types }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # CONTAINER
    # ─────────────────────────────────────────────
    - name: "Container: Scan"
      if: inputs.docker == 'true' || inputs.docker == 'auto' || inputs.container-image != ''
      shell: bash
      run: ${{ github.action_path }}/scripts/container-scanner.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        CONTAINER_IMAGE: ${{ inputs.container-image }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # DAST
    # ─────────────────────────────────────────────
    - name: "DAST: Scan"
      if: inputs.dast == 'true' && inputs.target-url != ''
      shell: bash
      run: ${{ github.action_path }}/scripts/dast-scanner.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        TARGET_URL: ${{ inputs.target-url }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # RECON
    # ─────────────────────────────────────────────
    - name: "Recon: Scan"
      if: inputs.recon == 'true' && inputs.target-url != ''
      shell: bash
      run: ${{ github.action_path }}/scripts/recon-scanner.sh
      env:
        TARGET_URL: ${{ inputs.target-url }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # DEPENDENCIES
    # ─────────────────────────────────────────────
    - name: "Dependencies: Audit"
      if: inputs.dependencies == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/deps-universal.sh
      env:
        FAIL_ON: ${{ inputs.fail-on }}
        LANGUAGES: ${{ inputs.languages }}
      continue-on-error: true

    - name: "Dependencies: Outdated"
      if: inputs.check-outdated == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/outdated-checker.sh
      env:
        LANGUAGES: ${{ inputs.languages }}
      continue-on-error: true

    - name: "Dependencies: Licenses"
      if: inputs.check-licenses == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/license-scanner.sh
      env:
        LANGUAGES: ${{ inputs.languages }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # QUALITY
    # ─────────────────────────────────────────────
    - name: "Quality: Metrics"
      if: inputs.quality == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/quality.sh
      env:
        LANGUAGES: ${{ inputs.languages }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # SBOM
    # ─────────────────────────────────────────────
    - name: "SBOM: Generate"
      if: inputs.sbom == 'true'
      shell: bash
      run: ${{ github.action_path }}/scripts/sbom-generator.sh
      env:
        SBOM_FORMAT: ${{ inputs.sbom-format }}
      continue-on-error: true

    # ─────────────────────────────────────────────
    # REPORTING
    # ─────────────────────────────────────────────
    - name: "Report: Summary"
      id: summary
      if: always()
      shell: bash
      run: ${{ github.action_path }}/scripts/summary.sh
      env:
        GITHUB_STEP_SUMMARY: ${{ env.GITHUB_STEP_SUMMARY }}
        FAIL_ON: ${{ inputs.fail-on }}

    - name: "Report: SARIF"
      id: sarif
      if: inputs.upload-sarif == 'true' && always()
      shell: bash
      run: ${{ github.action_path }}/scripts/sarif-upload.sh
      env:
        GITHUB_TOKEN: ${{ inputs.token }}
        REPORT_DIR: ${{ inputs.report-dir }}
      continue-on-error: true
